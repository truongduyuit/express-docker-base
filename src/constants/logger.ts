export enum g_logENV {
  console = 'default',
  file = 'file',
}

export enum g_logLevel {
  off = 'OFF',
  fatal = 'FATAL',
  error = 'ERROR',
  warn = 'WARN',
  info = 'INFO',
  debug = 'DEBUG',
  trace = 'TRACE',
  all = 'ALL',
}
